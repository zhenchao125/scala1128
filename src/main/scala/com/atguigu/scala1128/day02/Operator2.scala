package com.atguigu.scala1128.day02

/**
 * Author atguigu
 * Date 2020/4/21 9:09
 */
object Operator2 {
    def main(args: Array[String]): Unit = {
        var a = 10
        // 给a自增1, 使用赋值运算符
        a += 1
        println(a)
        
        // java: 三元运算符 true ? 1: 0, scala也去掉. 用if来替换
    }
}
